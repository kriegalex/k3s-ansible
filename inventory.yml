---
# Group names (server / agent / k3s_cluster) are hard-coded in the upstream
# playbooks — do not rename them.
server:
  hosts:
    k3s-server1:
      ansible_host: 10.0.0.10

agent:
  hosts:
    k3s-worker1:
      ansible_host: 10.0.0.11
    k3s-worker3:
      ansible_host: 10.0.0.13
    k3s-worker4:
      ansible_host: 10.0.0.14

k3s_cluster:
  children:
    server:
    agent:
