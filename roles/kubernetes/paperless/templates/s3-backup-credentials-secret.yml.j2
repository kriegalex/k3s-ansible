---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ paperless_postgresql_backup_credentials_secret_name }}"
  namespace: "{{ paperless_namespace }}"
type: Opaque
data:
  ACCESS_KEY_ID: "{{ paperless_postgresql_backup_s3_access_key | b64encode }}"
  ACCESS_SECRET_KEY: "{{ paperless_postgresql_backup_s3_secret_key | b64encode }}"
