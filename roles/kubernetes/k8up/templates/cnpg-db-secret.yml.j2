# Database credentials secret
apiVersion: v1
kind: Secret
metadata:
  name: "{{ restore_item.name }}-db-credentials"
  namespace: "{{ restore_item.namespace }}"
type: Opaque
stringData:
  username: "{{ restore_item.database_user }}"
  password: "{{ restore_item.database_password }}"