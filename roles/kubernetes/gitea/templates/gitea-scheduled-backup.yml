---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: gitea-scheduled-backup
  namespace: gitea
spec:
  schedule: "0 4 0 ? * MON"
  backupOwnerReference: self
  cluster:
    name: gitea-db
  immediate: true
