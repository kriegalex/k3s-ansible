---
apiVersion: v1
kind: Secret
metadata:
  name: "{{ gitea_postgresql_backup_credentials_secret_name }}"
  namespace: "{{ gitea_namespace }}"
type: Opaque
data:
  ACCESS_KEY_ID: "{{ gitea_postgresql_backup_s3_access_key | b64encode }}"
  ACCESS_SECRET_KEY: "{{ gitea_postgresql_backup_s3_secret_key | b64encode }}"
