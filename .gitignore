# Secrets — never commit
.vault_pass
group_vars/all/vault.yml

# Installed collections (requirements.yml is tracked)
collections/ansible_collections/

# Legacy techno-tim inventory kept on disk until migration is verified
inventory/

# Local ansible artifacts
.ansible/
*.retry
kubeconfig
collections/ansible_collections.broken-backup/
