[defaults]
inventory = inventory/hosts.yml  ; Adapt this to the path to your inventory file
roles_path = roles/k3s_cluster:roles/kubernetes:roles/backup_manager
vault_password_file = .vault_pass
collections_path = collections/
stdout_callback = ansible.builtin.default
result_format = yaml
